{
  "name": "Elysia Dev",
  "dockerComposeFile": "./compose.yml",
  "service": "app",
  "workspaceFolder": "/app",
  "mounts": [
    {
      "type": "bind",
      "source": "..",
      "target": "/app"
    },
  ],
  "postStartCommand": "bun install && bun run src/server.ts migrate",
  "customizations": {
    "vscode": {
      "extensions": [
        "oven.bun-vscode"
      ]
    },
    "codespaces": {
      "extensions": [
        "oven.bun-vscode"
      ]
    }
  },
  "remoteUser": "root",
  "remoteEnv": {
    "APP_DOMAIN": "localhost",
    "APP_VERSION": "0.0.0-devcontainer",
    "NODE_ENV": "devcontainer"
  },
  "runServices": [
    "postgres",
    "mailpit",
    "jaeger"
  ]
}
