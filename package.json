{
  "name": "learn-elysia",
  "description": "Learning Elysia.js and Bun.js",
  "version": "0.0.3",
  "module": "dist/server.js",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/server.ts --minify-whitespace --minify-syntax --sourcemap --target bun --outdir ./dist",
    "db:generate": "bunx --bun drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio --port 4000",
    "dev": "bun run --watch src/server.ts",
    "dev:debug": "bun run --inspect=ws://localhost:6499 src/server.ts",
    "format": "biome format --write",
    "lint": "biome check",
    "postinstall": "if [ -z \"$CI\" ]; then simple-git-hooks; fi",
    "release": "bunx commit-and-tag-version -s",
    "start": "bun dist/server.js",
    "test:debug": "bun --inspect=ws://localhost:6499 test"
  },
  "dependencies": {
    "@elysiajs/openapi": "^1.4.13",
    "@elysiajs/static": "^1.4.7",
    "better-auth": "^1.4.11",
    "commander": "^14.0.2",
    "drizzle-orm": "^0.45.1",
    "drizzle-typebox": "^0.3.3",
    "elysia": "^1.4.22",
    "pino": "^10.3.0",
    "pino-pretty": "^13.1.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.13",
    "@types/bun": "1.2.20",
    "@types/node": "24.9.1",
    "drizzle-kit": "^0.31.8",
    "lint-staged": "^16.2.7",
    "openapi-types": "^12.1.3",
    "pg": "^8.17.0",
    "simple-git-hooks": "^2.13.1"
  },
  "peerDependencies": {
    "@sinclair/typebox": "^0.34.41",
    "typescript": "^5.9.3"
  },
  "simple-git-hooks": {
    "pre-commit": "bunx lint-staged --allow-empty"
  },
  "lint-staged": {
    ".github/**/*.yml": [
      "biome format --write --no-errors-on-unmatched"
    ],
    "{database,src,test}/**/*.ts": [
      "biome format --write --no-errors-on-unmatched"
    ],
    "*.{ts,json,jsonc}": [
      "biome format --write --no-errors-on-unmatched"
    ]
  }
}
